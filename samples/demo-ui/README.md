# Demo UI 示例组件库

一组可入库的 Vue 3 `<script setup>` 组件，用于 CompiraMCP 演示解析 Props / Events / Slots / v-model。

| 组件 | 说明 |
|------|------|
| CmpButton | 按钮 |
| CmpInput | 输入框（v-model） |
| CmpCard | 卡片（具名插槽） |
| CmpSwitch | 开关（v-model） |
| CmpTag | 标签 |
